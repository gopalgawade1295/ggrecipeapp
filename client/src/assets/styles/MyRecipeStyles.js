import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const RecipeImgBox = styled(Box)({
    width: '100%',
    maxHeight: '350px',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'flex-start',
    overflow: 'hidden',
    marginBottom: '4px'
})

export const RecipeIngredientsBox = styled(Box)({
    width: '100%',
    display: 'flex'
})
